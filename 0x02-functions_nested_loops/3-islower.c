#include "main.h"

/**
 * islower - checks for lowercase character
 * @c: a character argument
 * Return: retuens 0 and 1 depending on condition
 */
int _islower(int c);
{

	return (c >= 'a' && c <= 'z');
}
